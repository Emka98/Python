Create a list and insert ten arbitrary integers into it. Create a second list that initially contains the even numbers from the first list, followed by the remaining numbers. Display both lists on separate lines.